class Media {
    constructor(name, duration) {
      this.name = name;
      this.duration = duration;
    }
}

    class music extends media {
        constructor(artist){
          this.artist = artist;
        }
    }

  
    class podcast extends media

      

    class movie extends media{
        constructor(rating){
          this.rating = rating;
        }
    }
  

 // Method
function next() { 
next.staticMethod = function() {
  return(this === next);
};
// Method
function play() { }
play.staticMethod = function() {
  return(this === play);
};

Media.sort(function (a, b) {
  return list;      
}